#include<iostream>
#include<queue>
using namespace std;
int main(){
    /*int x[5]={1,2,10,8,13};
    priority_queue<int>var;
    for(int i=0;i<5;i++){
        var.push(x[i]);
    }
    while(!var.empty()){
        cout<<var.top()<<" ";//in priority queue we have to use top()function rather than front cause it sort data in high to low order
        var.pop();
    }*/
   ///min to max:
   
}